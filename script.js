document.querySelectorAll(".filter").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".filter").forEach(b=>b.classList.remove("active"));btn.classList.add("active");const f=btn.dataset.filter;document.querySelectorAll(".video-card").forEach(card=>card.style.display=f==="all"||card.dataset.category===f?"block":"none")}));
function sendMessage(e){e.preventDefault();const name=document.getElementById("name").value;document.getElementById("status").textContent=`Merci ${name} ! Le formulaire est prêt à être connecté à votre e-mail ou WhatsApp.`;e.target.reset();}
